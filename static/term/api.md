# API

**API**（Application Programming Interface，应用程序接口）是不同软件系统之间通信的桥梁。

## 常见类型

- **REST API**：基于 HTTP 协议，使用 JSON 交换数据
- **GraphQL**：由 Facebook 开发，客户端可精确指定所需数据
- **WebSocket**：全双工通信，适合实时场景

## 为什么重要

> 没有 API，现代互联网应用将无法互联互通。

开发者通过调用 API，无需了解内部实现即可使用第三方服务（如支付、地图、短信等）。
